# Examples

Runnable examples and integration snippets belong here.

- `agora_conversational_ai/` contains the example bridge/backend flow for testing the first-party `agora-convoai` gateway module against Synapse. Its example browser client now lives in the repo-root `exmaple-ui/`, while the reusable gateway backend logic lives under `src/synapse/gateways/agora_convoai/`.
