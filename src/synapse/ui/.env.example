# Public HTTPS base URL for the Synapse backend when this UI is deployed on a
# different origin, such as Vercel. Leave unset for local same-origin / Vite
# proxy development.
VITE_API_BASE_URL=https://your-synapse-backend.example.com
